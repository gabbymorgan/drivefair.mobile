import React from 'react';
import {Icon} from '@ui-kitten/components';

export const NavigateIcon = (props) => (
  <Icon {...props} name="compass-outline" width={30} height={30} fill="white" />
);
export const CloseCircleIcon = (props) => (
  <Icon
    {...props}
    name="close-circle-outline"
    width={20}
    height={20}
    fill="white"
  />
);
